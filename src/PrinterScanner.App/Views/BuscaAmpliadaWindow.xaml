<Window x:Class="PrinterScanner.App.Views.BuscaAmpliadaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Busca Ampliada — UP Tecnologias"
        Width="620" Height="500"
        MinWidth="480" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBg}"
        Loaded="Window_Loaded">

  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*"    />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <StackPanel Grid.Row="0" Margin="0,0,0,12">
      <TextBlock Text="Busca Ampliada"
                 FontSize="16" FontWeight="SemiBold"
                 Foreground="{DynamicResource TextPrimary}" />
      <TextBlock Margin="0,3,0,0"
                 Text="Localiza impressoras em sub-redes adjacentes adicionando IPs temporarios a placa de rede."
                 FontSize="11"
                 Foreground="{DynamicResource TextSecondary}"
                 TextWrapping="Wrap" />
    </StackPanel>

    <!-- Progress bar -->
    <Grid Grid.Row="1" Margin="0,0,0,10">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="60" />
      </Grid.ColumnDefinitions>
      <ProgressBar Grid.Column="0" Height="14"
                   Minimum="0" Maximum="100"
                   Value="{Binding ProgressValue, Mode=OneWay}" />
      <TextBlock Grid.Column="1" Margin="8,0,0,0"
                 VerticalAlignment="Center"
                 FontWeight="SemiBold"
                 Foreground="{DynamicResource Accent}"
                 Text="{Binding ProgressText, Mode=OneWay}" />
    </Grid>

    <!-- Log -->
    <Border Grid.Row="2"
            BorderBrush="{DynamicResource PanelBorder}"
            BorderThickness="1"
            Background="{DynamicResource CardBg}">
      <ListBox x:Name="LogList"
               ItemsSource="{Binding BuscaAmpliadaLog}"
               Background="Transparent"
               BorderThickness="0"
               Padding="6,4"
               FontFamily="Consolas"
               FontSize="12"
               ScrollViewer.HorizontalScrollBarVisibility="Auto"
               ScrollViewer.VerticalScrollBarVisibility="Auto">
        <ListBox.ItemContainerStyle>
          <Style TargetType="ListBoxItem">
            <Setter Property="Padding"    Value="2,1" />
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                  <ContentPresenter Margin="{TemplateBinding Padding}" />
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ListBox.ItemContainerStyle>
      </ListBox>
    </Border>

    <!-- Buttons -->
    <Grid Grid.Row="3" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0" VerticalAlignment="Center"
                 FontSize="11"
                 Foreground="{DynamicResource TextSecondary}"
                 Text="{Binding StatusMessage, Mode=OneWay}"
                 TextTrimming="CharacterEllipsis" />

      <!-- BroadcastScanCommand já tem CanExecute = !IsScanning -->
      <Button Grid.Column="1"
              Content="Buscar novamente"
              Margin="0,0,8,0" Padding="14,7"
              Command="{Binding BroadcastScanCommand}"
              Click="BtnBuscarNovamente_Click">
        <Button.Style>
          <Style TargetType="Button">
            <Setter Property="Background"     Value="{DynamicResource PrimaryButtonBg}" />
            <Setter Property="BorderBrush"    Value="{DynamicResource PrimaryButtonBorder}" />
            <Setter Property="Foreground"     Value="White" />
            <Setter Property="FontWeight"     Value="SemiBold" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </Button.Style>
      </Button>

      <!-- CancelScanCommand já tem CanExecute = IsScanning -->
      <Button Grid.Column="2"
              Content="Cancelar e restaurar DHCP"
              Margin="0,0,8,0" Padding="14,7"
              Command="{Binding CancelScanCommand}">
        <Button.Style>
          <Style TargetType="Button">
            <Setter Property="Background"     Value="#E53935" />
            <Setter Property="BorderBrush"    Value="#C62828" />
            <Setter Property="Foreground"     Value="White" />
            <Setter Property="FontWeight"     Value="SemiBold" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </Button.Style>
      </Button>

      <Button Grid.Column="3"
              Content="Fechar"
              Padding="14,7"
              Click="BtnFechar_Click">
        <Button.Style>
          <Style TargetType="Button">
            <Setter Property="Background"     Value="{DynamicResource ButtonBg}" />
            <Setter Property="BorderBrush"    Value="{DynamicResource ButtonBorder}" />
            <Setter Property="Foreground"     Value="{DynamicResource ButtonFg}" />
            <Setter Property="BorderThickness" Value="1" />
            <Style.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </Button.Style>
      </Button>
    </Grid>
  </Grid>

</Window>
