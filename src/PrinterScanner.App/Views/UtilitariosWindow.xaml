<Window x:Class="PrinterScanner.App.Views.UtilitariosWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Utilitarios de Impressora"
        Width="560" Height="460"
        MinWidth="480" MinHeight="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource AppBg}">

  <Window.Resources>
    <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
  </Window.Resources>

  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <!-- Header -->
    <Border Grid.Row="0"
            Padding="12,10"
            Margin="0,0,0,12"
            Background="{DynamicResource PanelBg}"
            BorderBrush="{DynamicResource PanelBorder}"
            BorderThickness="1">
      <StackPanel>
        <TextBlock FontSize="15" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextPrimary}"
                   Text="Utilitarios de Impressora" />
        <TextBlock Margin="0,3,0,0"
                   Foreground="{DynamicResource TextSecondary}"
                   Text="Selecione o utilitario desejado e clique em Abrir, ou clique duas vezes no item." />
      </StackPanel>
    </Border>

    <!-- Lista de utilitários -->
    <Grid Grid.Row="1">

      <!-- Mensagem quando não há utilitários -->
      <Border Padding="20"
              Background="{DynamicResource CardBg}"
              BorderBrush="{DynamicResource PanelBorder}"
              BorderThickness="1"
              Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock HorizontalAlignment="Center"
                     FontSize="14"
                     FontWeight="SemiBold"
                     Foreground="{DynamicResource TextSecondary}"
                     Text="Nenhum utilitario disponivel" />
          <TextBlock Margin="0,8,0,0"
                     HorizontalAlignment="Center"
                     TextAlignment="Center"
                     Foreground="{DynamicResource TextSecondary}"
                     TextWrapping="Wrap"
                     Text="Esta versao do aplicativo nao inclui utilitarios.&#x0a;Use a versao de instalacao local para acessar esta funcionalidade." />
        </StackPanel>
      </Border>

      <!-- Lista quando há utilitários -->
      <Border Background="{DynamicResource CardBg}"
              BorderBrush="{DynamicResource PanelBorder}"
              BorderThickness="1"
              Visibility="{Binding HasUtilitarios, Converter={StaticResource BoolToVisibility}}">
        <ListView ItemsSource="{Binding Utilitarios}"
                  SelectedItem="{Binding SelectedUtilitario}"
                  BorderThickness="0"
                  Background="Transparent"
                  Foreground="{DynamicResource TextPrimary}"
                  MouseDoubleClick="ListView_MouseDoubleClick"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
          <ListView.ItemContainerStyle>
            <Style TargetType="ListViewItem">
              <Setter Property="HorizontalContentAlignment" Value="Stretch" />
              <Setter Property="Padding" Value="12,10" />
              <Setter Property="Foreground" Value="{DynamicResource TextPrimary}" />
              <Setter Property="Background" Value="Transparent" />
              <Setter Property="BorderThickness" Value="0,0,0,1" />
              <Setter Property="BorderBrush" Value="{DynamicResource GridHLine}" />
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="ListViewItem">
                    <Border x:Name="ItemBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                      <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                      <Trigger Property="IsMouseOver" Value="True">
                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource GridAltRowBg}" />
                      </Trigger>
                      <Trigger Property="IsSelected" Value="True">
                        <Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource Accent}" />
                        <Setter Property="Foreground" Value="White" />
                      </Trigger>
                    </ControlTemplate.Triggers>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </ListView.ItemContainerStyle>
          <ListView.ItemTemplate>
            <DataTemplate>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                  <TextBlock FontWeight="SemiBold"
                             FontSize="13"
                             Text="{Binding Name}"
                             Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" />
                  <TextBlock Margin="0,2,0,0"
                             FontSize="11"
                             Opacity="0.7"
                             Text="{Binding ExeFileName}"
                             Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" />
                </StackPanel>
                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           FontSize="18"
                           Opacity="0.4"
                           Text="›"
                           Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=Foreground}" />
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </Border>
    </Grid>

    <!-- Botões -->
    <Grid Grid.Row="2" Margin="0,12,0,0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBlock Grid.Column="0"
                 VerticalAlignment="Center"
                 Foreground="{DynamicResource TextSecondary}"
                 FontSize="11">
        <Run Text="Pasta:" />
        <Run Text="{Binding SelectedUtilitario.FolderPath, FallbackValue=''}" />
      </TextBlock>

      <Button Grid.Column="1"
              Content="Abrir"
              Command="{Binding AbrirCommand}"
              MinWidth="100"
              Padding="14,7"
              Margin="0,0,8,0"
              FontWeight="SemiBold"
              Foreground="White"
              Background="{DynamicResource PrimaryButtonBg}"
              BorderBrush="{DynamicResource PrimaryButtonBorder}"
              BorderThickness="1">
        <Button.Style>
          <Style TargetType="Button">
            <Setter Property="Background"   Value="{DynamicResource PrimaryButtonBg}" />
            <Setter Property="BorderBrush"  Value="{DynamicResource PrimaryButtonBorder}" />
            <Setter Property="Foreground"   Value="White" />
            <Setter Property="FontWeight"   Value="SemiBold" />
            <Setter Property="Padding"      Value="14,7" />
            <Setter Property="MinWidth"     Value="100" />
            <Setter Property="Margin"       Value="0,0,8,0" />
            <Style.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.38" />
              </Trigger>
            </Style.Triggers>
          </Style>
        </Button.Style>
      </Button>

      <Button Grid.Column="2"
              Content="Fechar"
              IsCancel="True"
              Click="BtnFechar_Click"
              MinWidth="100"
              Padding="14,7"
              Background="{DynamicResource ButtonBg}"
              BorderBrush="{DynamicResource ButtonBorder}"
              Foreground="{DynamicResource ButtonFg}"
              BorderThickness="1">
        <Button.Style>
          <Style TargetType="Button">
            <Setter Property="Background"     Value="{DynamicResource ButtonBg}" />
            <Setter Property="BorderBrush"    Value="{DynamicResource ButtonBorder}" />
            <Setter Property="Foreground"     Value="{DynamicResource ButtonFg}" />
            <Setter Property="Padding"        Value="14,7" />
            <Setter Property="MinWidth"       Value="100" />
          </Style>
        </Button.Style>
      </Button>
    </Grid>
  </Grid>
</Window>
